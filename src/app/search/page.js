import React from 'react'

const page = () => {
  return (
    <div>search page


        {/* <ProductCard
              key={item.id}
              item={item}
              variant={variant}
              cartItem={cartItem}
              addToCart={addToCart}
              updateQty={updateQty}
              onVariantChange={(v) =>
                setSelectedVariants((prev) => ({
                  ...prev,
                  [item.id]: v,
                }))
              }
            /> */}
    </div>
  )
}

export default page